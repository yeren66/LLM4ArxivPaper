"""Placeholder for ar5iv parsing utilities."""

from __future__ import annotations

from typing import Optional


def fetch_markdown(arxiv_id: str) -> Optional[str]:
    """Return markdown representation of a paper.

    A full parser will be ported from the legacy implementation in later
    iterations. Currently returns ``None`` as a stub.
    """

    return None
